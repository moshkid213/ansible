- hosts: Routers

  gather_facts: false
  connection: local

  tasks:
   - name: NoShut Interface
     ios_commands:
       commands:
         - config t
         - interface f0/1
         - no shut

  
